# IncEventGS run config stub generated by convert_to_inceventgs.py
# Edit datadir/output, then run: python main.py --config <this file>
# NOTE: dataset class below is a placeholder -- a pure event-only custom capture
# needs a custom dataset class (see converter output notes). Intrinsics live HERE,
# not in the dataset dir.
inherit_from: configs/SimuEvent/simu_event.yaml
dataset: 'replica_simu_event'   # placeholder; loader requires images+poses (see notes)
polarity_offset: 0.0            # converter already wrote signed {-1,+1} polarity

data:
  datadir: /home/ubuntu/Documents/Development/ThinkCam/datasets/thinkcam_capture
  scene: thinkcam_capture
  output: output/thinkcam_capture
  exp_name: demo
  downsample_factor: 1

cam:
  H: 720
  W: 1280
  fx: FIXME_REQUIRES_CALIBRATION
  fy: FIXME_REQUIRES_CALIBRATION
  cx: 640.0
  cy: 360.0
  # calibrated: False  (False => fx/fy are placeholders; calibrate the camera)

# event_threshold dir used: event_threshold_0.1
